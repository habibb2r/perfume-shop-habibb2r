

const SectionTitle = ({title}) => {
    return (
        <div className="w-full flex justify-center items-center">
            <h1 className="font-title font-semibold text-4xl text-center w-1/5">{title}</h1>
        </div>
    );
};

export default SectionTitle;